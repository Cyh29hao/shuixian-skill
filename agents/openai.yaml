interface:
  display_name: "水仙.skill"
  short_description: "Build a self-mirror companion from chat logs, values, relationship patterns, and the user's private voice."
  default_prompt: "Use $create-shuixian to build a less-generic self-mirror companion from my chat logs, speaking habits, values, and relationship patterns. Start by telling me how close the current source material can get."

policy:
  allow_implicit_invocation: true
